cp create destination dir if it does not exist
copy and create destination dir if it does not exist
cp command create any necessary folders for copying a file to a destination

the special Bash variable $_. 
The underscore variable $_ expands to the last argument of the last command executed.
 
mkdir -p "$d" && cp file "$d"
mkdir -p /foo/bar && cp myfile "$_"

mkdir -p /tmp/test/one/non-exist/backup/dir && cp $HOME/.vimrc $_/myVimrc

source C:/bash/func.sh 

CP implementation/brts-parent/brts-war/src/test/java/ca/gc/cra/brts/web/common/ejb/TestRunEJB.java C:/BRTSv2-2023/2022-10-28/implementation/brts-parent/brts-war/src/test/java/ca/gc/cra/brts/web/common/ejb


C:\bash